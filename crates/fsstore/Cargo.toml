[package]
name = "fsstore"
version = "0.0.0"
edition = "2021"
description = "共有フォルダ向けのアトミック保存と助言ロック（docs/05-persistence.md §3）。Tauri 殻が呼ぶ純Rust層。"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
